 import numpy as np
 import says

 #define a function that returns a value 
 def expo(x)：
 return np.exp(x)   #return the np e^x function

 #define a subroutine that do not return value
 for i in range(n)
  print(expo(float(i))   #call the expo function

  #define a main function 
   def main()
       n=10  #provide a default value for n

       #check if there is a command line argument provide
       if(len(sys.argv)>1):
          n=int(sys.argv[1])  if an argument was provided, use it for n

          show expo(n)     #call the show_expo subroutine.


        #run the main function
        if _name_ == "_main_":
           main()